[package]
name        = "paperclaw-domain"
version     = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true

description = "Pure types and trait ports for PaperClaw — no I/O."

[features]
default = []
# In-memory fakes for use-case and adapter tests. Opt in from dev-dependencies.
testing = []

[dependencies]
serde       = { workspace = true }
thiserror   = { workspace = true }
async-trait = { workspace = true }
strum       = { workspace = true }
time        = { workspace = true }
uuid        = { workspace = true }

[lints]
workspace = true
